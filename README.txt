Note: SIERPASCII.F is written in fortran 77. To compile it, you must run:

---
gfortran -std=legacy PROGRAM.exe SRC/SIERPASCII.F
---

An issue with this is that a prompt will **NOT** show up if you pipe the result to a file. However, it will still accept user input.
